import { ActionType } from 'typesafe-actions';
import * as actions from './action';

export interface decriptionStates {
   
}

/* --- ACTIONS --- */
type DescriptionPageActions = ActionType<typeof actions>;


export type ContainerActions = DescriptionPageActions;

// export default ContainerActions;